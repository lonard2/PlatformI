---
target: admin surface
total_score: 25
max_score: 40
na_heuristics: 
p0_count: 0
p1_count: 0
note: fresh rubric, incomparable to earlier runs per assessor; item-level 8/8 FIXED
target_identity: "file:/Users/lonard/Desktop/PlatformI/src-app-admin"
timestamp: 2026-09-03T14-04-14Z
slug: src-app-admin
---
# Re-Critique: Admin surface (post i18n-residue + event-stream + perf/focus cycle)

**Score: 25/40 — fresh rubric, INCOMPARABLE to earlier runs per assessor** | Detector: 0 | Baseline: 8/8 FIXED | P0: 0, P1: 0
A's note: item-level verdicts are the real result; polish genuinely rose; capability floors (help 1, recall 2, efficiency 2, control breadth 2) cap the scalar by construction.

## Baseline verification (8/8 FIXED, code-verified)
Fleet aria keys x6; heading key reuse; Juta -> Intl.NumberFormat(i18n locale, compact); event stream on simulation clock + WIB + recency sort; 300ms debounced URL sync (restore intact); keepalive + pagehide flush; natural-expiry focus -> feed container; unified :focus-visible rule.

## Regressions found in the fixes
- pagehide + unmount double-fire on hard reload (second DELETE 404s -> restore-mid-unload); bfcache ghost (no event.persisted check): restore-after-bfcache resurrects server-deleted row.
- Doubled focus indicators: global outline + surviving per-element rings, mismatched geometry.
- Redundant router.replace on mount (no equality guard); browser Back does not restore filters.

## Priority Issues
- [P2] pagehide: event.persisted guard + flushed-set shared by both paths.
- [P2] Focus ring sweep: delete surviving per-element focus-visible:ring-* classes.
- [P2] router.replace equality guard; Back-button filter restore (sync state from searchParams on popstate).
- [P2] Undo breadth: route status/crowd/escalate/resolve through the grace engine (highest-leverage).
- [P3] Locale discipline: scanner id-ID pin, alerts default-locale time, untranslated severity enums; paused-clock "Active" badge; py-0.2 invalid class; h-screen vs dvh; no RTL for ar.

## Strengths
- Delete-grace engine: pause defers the real deletion; focus choreography complete both directions.
- Cross-surface grammar: admin mirrors passenger drawer + inspector vocabulary.
- Operational-trust posture: shared uptime formula, failure restores state, stale announced.

## Ceiling (unchanged, biting)
Shift log; triage queue; consequence preview; per-page composition; keyboard layer. Polish cycles now produce flat scores.
