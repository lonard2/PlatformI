---
target: admin surface
total_score: 24
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 4
timestamp: 2026-09-01T13-43-31Z
slug: src-app-admin
---
# Re-Critique: Admin surface (post harden/clarify/polish cycle)

**Score: 24/40 — Poor (borderline)** (19 → 24) | Method: dual-agent (design review + detector)

## Baseline verification (code-verified)
FIXED: label cluster (6/6), DELETE undo (5s + aria), keyboard rows, dashboard KPIs wired to store, reduced-motion guards, dead imports 29→5, aria-live 0→5, th scope 0→14, htmlFor 0→13.
PARTIAL: modals (ARIA+Escape yes, trap/restore/backdrop no), failure surfaces (broadcast+GET yes, resolve/escalate/delete no), dashboard (fleet live, disruption KPI static).

## Priority Issues
- [P0] /api/alerts POST/PATCH/DELETE unauthenticated — middleware matcher excludes it; anonymous fake-CRITICAL broadcast + log wipe possible.
- [P1] Passkeys ship in client bundle (REGISTERED_OPERATORS imported by login page, presets auto-fill password).
- [P1] Passkey compare `!==` not constant-time (constantTimeEqual exists unused).
- [P1] Fleet dialogs: no focus trap, no focus restore, no backdrop close.
- [P1] Dashboard disruption KPI reads static DISRUPTION_ALERTS, diverges from /api/alerts after broadcast.
- [P2] Resolve/escalate/delete failures console-only; double-delete orphan (timer cancel resurrects first alert).
- [P2] Residual labels: fleet "Affected Line" header, scanner "Actions: {code}", "Ticket Expired" vs window-expired.
- [P2] role=button on tr breaks SR table semantics.
- [P2] Auth debts: 200-line hand-rolled HMAC vs WebCrypto, hardcoded fallback secret, Bearer header accepted on page routes.
- [P3] Login polish: unlabeled inputs, no autoComplete, no aria-live, text-[8px]; timeouts uncleaned on unmount; 5 dead imports; text-[9px] x6; unbounded scanner audit log; select-none shell blocks copy; hex drift.

## Strengths
- Broadcast workflow exemplary: live preview mirror, undo delete, error banners keep draft.
- Severity semantics system-wide consistent with passenger drawer + DESIGN.md.
- Scanner presets: self-documenting failure-mode buttons, better than help panel.
