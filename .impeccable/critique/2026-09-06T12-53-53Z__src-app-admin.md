---
target: admin surface
total_score: 31
max_score: 40
na_heuristics: 
p0_count: 2
p1_count: 2
target_identity: "file:/Users/lonard/Desktop/PlatformI/src-app-admin"
timestamp: 2026-09-06T12-53-53Z
slug: src-app-admin
---
# Re-Critique: Admin surface (cycle 13 — prior commands + beyond: shortcuts, help modal, shift log)

**Score: 31/40 — Good** (29 -> 31, assessor scored 30.5) | Detector: 5 (4 tab-underline false positives, 1 real 9px advisory) | Cognitive load 7/8 | P0: 2, P1: 2

## Design Health Score (Nielsen rubric)

| # | Heuristic | Score | Key Issue |
|---|-----------|-------|-----------|
| 1 | Visibility of System Status | 4/4 | role=status undo banners, pausable countdowns, stale banners, aria-busy, in-modal undo, shift-log badge count. |
| 2 | Match System / Real World | 3/4 | Transit-native vocabulary; shift-log summaries embed untranslated English connectives. |
| 3 | User Control and Freedom | 3/4 | Undo covers status/crowd/add + resolve/demote/reopen + delete, all pausable; clearShiftLog is one-click irreversible. |
| 4 | Consistency and Standards | 3/4 | Semantic undo theming (amber/cyan) as a house rule; help modal hardcodes English section titles. |
| 5 | Error Prevention | 3/4 | Two-step escalation, required-field disable, clamps; hotkeys don't veto while dialogs are open. |
| 6 | Recognition Rather Than Recall | 3/4 | ? kbd chip pattern; / behavior (first text input anywhere) mismatches the cheatsheet promise. |
| 7 | Flexibility and Efficiency | 3/4 | Global 1-4, /, ?, Esc; no undo hotkey (Ctrl+Z), no row-level jump, no suppression under dialogs. |
| 8 | Aesthetic and Minimalist Design | 3/4 | Calm KPI grid with high-contrast values; synthesized event-stream pings pose as an OCC feed. |
| 9 | Recognize/Recover from Errors | 3/4 | Reverse-PATCH undos, optimistic rollback, keepalive deletes, pagehide flush; dialog-layer faults dock it. |
| 10 | Help and Documentation | 3/4 | Four-panel modal in six languages, above admin-console baseline; broken for keyboard users via the trap bug. |
| **Total** | | **31/40** | **Good — H7 flexibility and H10 help moved from ~1/2 to 3** |

## Cognitive Load Checklist (8 items)

| # | Item | Verdict |
|---|------|---------|
| 1 | Fleet at a glance | PASS — category pills with counts, line-colored badges, sticky actions column. |
| 2 | What needs action first | PASS — alerts triage order, dashboard critical/warning split, hero quick-links. |
| 3 | Author + verify broadcast | PASS — live banner + drawer preview with real line colors before send. |
| 4 | Escalate safely keyboard-only | PASS — inline confirm, focus moves to Cancel, Esc restores trigger, disconnected-trigger fallback. |
| 5 | Undo a mistake (all mutations) | PASS — FLEET_ADD registered in the same field-scoped registry with amber theming, in-modal banner, stale-mutation clearing. |
| 6 | Trust the numbers | PASS — Fleet Not Held / Est. Occupied-Cabin Value with basis notes in all 6 locales. |
| 7 | Recover when backend down | PASS — retry affordances, HTTP-status + cached-state messaging, row resurrection on failed deletes. |
| 8 | Hand over the shift | FAIL (partial) — log exists, persists, records every action type incl. GATE_SCAN; but no shift identity, no operator attribution, no export, unguarded clear. |

## Synthesis Points

| Aspect | Summary |
|--------|---------|
| Method | Dual-agent independent critique (design-director + deterministic detector), source-only; browser automation unavailable. |
| Assessors | A = design review + regression probe of six commits; B = detector + prior-baseline table + new-functionality verification + shortcut edge cases. |
| Detector | 5 findings: 4 border-accent-on-rounded warnings = intentional tab-underline pattern (false positives); 1 valid text-[9px] advisory. |
| Score arc | 19 -> 24 -> 26 -> 29 -> 29 -> 31 -> 28 -> 28 -> 25 (fresh rubric) -> 27 -> 29 -> 31. Movement: H7 flexibility ~1->3, H10 help ~2->3. |
| Strongest dimension | Visibility of System Status 4/4 — ambient, honest, channeled through header, nav, banner, and shift-log badge. |
| Weakest dimensions | Help & Documentation history lifted to 3; remaining caps: shift-log identity, consequence preview, per-page composition. |
| Emotional arc | Confident -> trusting -> careful -> relieved -> *almost* signed-off: the log gives footprints, but the ending is an unprotected delete key. |
| Best interaction | The undo family: field-scoped, pausable, in-modal, semantically themed — a coherently designed subsystem, not per-page copies. |
| Highest-leverage fixes | Help modal trap wiring (2 lines), dialog-overlay hotkey gate, shift-log identity + guarded clear + export. |
| Ceiling to 35+ | True shift identity (operator attribution + start/end framing + export), consequence preview, per-page composition. The shift log this cycle built is the foundation for the last item. |


## Baseline verification (all prior commands executed and verified)
KPI retitles + basis notes x6; fleet field-scoped undo substance (telemetry preserved, no rewind) + in-modal banner + escalate clears pendingMutations; Cancel focus; localized status vocabulary; ring fallback; 44px select; dead branch; scan-count word; past-tense toasts.

## New functionality verdict
- Shortcuts: FIXED (1-4 route, / focus, ? help, Esc) with gaps — not suppressed under open dialogs; / targets first text input (broadcast title on alerts), false on 3/4 pages vs cheatsheet copy.
- Help modal: PARTIAL — dialog grammar + focus save/restore + i18n x6 complete + excellent content (shortcuts/triage/ops/shift-log panels); Tab trap NOT attached (handleTrapKeyDown destructured but never wired).
- Shift log: FIXED core / PARTIAL detail — 10 action types, localStorage, 100-entry retention, type-tinted badges, survives navigation; but hardcoded operatorId "OCC-DISPATCHER", no shift framing, unguarded one-click clearShiftLog, no export, summaries embed English connectives, undo events not recorded.
- Add-vehicle undo: FIXED (5s window, in-modal, field-scoped pattern).

## Priority Issues
- [P0] Help modal Tab trap not wired (handleTrapKeyDown destructured, never bound) — aria-modal claims what the trap doesn't enforce. 2-line diff.
- [P0] Hotkeys ignore open dialogs: 1-4 navigate behind the help modal; / focuses background inputs. Fix: dialog overlay count gate in the shared handler.
- [P1] Shift log identity: capture real operator at record time; shift start/end framing; confirm dialog on clear; copy-summary export button.
- [P1] / hotkey targets wrong input on 3/4 pages — data-hotkey-search marking.
- [P2] Mixed-language shift-log summaries (English connectives) — template strings per ShiftActionType, applied at render.
- [P2] Event stream fabricates timestamps from speed (pingOffsetMs = idx*8000 + speed*17 % 5000) — label as "Fleet sample" or drive from real store ticks.
- [P3] Login route hotkey listener before auth guard; help modal 4-tab mobile wrap; text-[9px] badge; AZERTY digit misfire; undo events not in shift log; add-vehicle mode-dependent coachbuilder seeding.

## Strengths
- Undo family coherently designed: field-scoped, pausable, in-modal variant, semantic amber/cyan theming as an applied standard.
- Trust engineering: basis notes under KPI values; honest em-dash empty states.
- Help modal documents WHY (WCAG 2.2.1 pause rationale, JakLingko cap) in six languages.

## Cognitive load: 7/8 (items 5+6 now PASS; item 8 partial — mechanism exists, identity missing)
## Ceiling
Remaining: true shift identity (operator + start/end framing + export), consequence preview, per-page composition. The shift log mechanism this cycle built is the foundation for the last ceiling item.
